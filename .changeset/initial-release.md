---
"thaipass": minor
---

First release of the AI SDK provider for AI Pass: `createAipass` gives `streamText` and `generateText` the models an account already sees, with attachments, reasoning levels, tool calling over text and the credit balance in `providerMetadata`.
